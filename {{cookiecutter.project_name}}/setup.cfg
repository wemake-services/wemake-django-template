# All configuration for plugins and other utils is defined here.
# Read more about `setup.cfg`:
# https://docs.python.org/3.11/distutils/configfile.html


[flake8]
# flake8 configuration:
# https://flake8.pycqa.org/en/latest/user/configuration.html
format = wemake
show-source = true
statistics = false
doctests = true

# Flake plugins:
max-line-length = 80
max-complexity = 6

extend-exclude = .venv

# We only run wemake-python-styleguide and system checks:
select = WPS, E99

# Docs: https://github.com/snoack/flake8-per-file-ignores
# You can completely or partially disable our custom checks,
# to do so you have to ignore `WPS` letters for all python files:
per-file-ignores =
  # Allow some magic in `implemented`:
  server/implemented.py: WPS202, WPS213, WPS235
  # Allow `__init__.py` with logic for configuration:
  server/settings/*.py: WPS226, WPS407, WPS412, WPS432
  # Allow to have magic numbers inside migrations and wrong module names:
  server/*/migrations/*.py: WPS102, WPS432
  # Allow a lot of imports in `urls.py` and `views.py`:
  server/urls.py: WPS201
  server/apps/main/api/views.py: WPS201
  # Enable `assert` keyword and magic numbers for tests:
  tests/*.py: WPS432
