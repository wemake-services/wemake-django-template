- repo: local
  hooks:
    - id: gitlint
      name: gitlint
      entry: "bash -c 'gitlint lint'"
      language: system

    - id: pytest
      name: pytest
      entry: python manage.py test
      language: system

    - id: safety
      name: safety
      entry: safety check
      language: system
